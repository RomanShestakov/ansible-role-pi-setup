# meta/main.yml

galaxy_info:
  author: Roman Shestakov
  description: configure raspberry pi
  min_ansible_version: 1.4
  license: MIT
  platforms:
  - name: Ubuntu
    versions:
    - all
  categories:
  - system

dependencies:
  - { role: "knopki.locale", version: "v1.0.3", locale_all: "{{raspi_config_locale}}" }
